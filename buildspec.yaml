version: 0.2
services:
  nilesh:
    install:
      runtime-versions:
      docker: 18
    build: ./
    command: python3 docrun.py
artifacts:
  files:
    - "**/*"
  name: "web-dist-${env}"
  base-directory: build
# version: 0.2
#       phases:
#         install:
#          runtime-versions:
#          docker: 18
